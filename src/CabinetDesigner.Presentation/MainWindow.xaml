<Window x:Class="CabinetDesigner.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:CabinetDesigner.Presentation.Views"
        Title="{Binding WindowTitle}"
        Width="1500"
        Height="960"
        MinWidth="1280"
        MinHeight="840"
        Background="{StaticResource ShellWindowBrush}"
        FontFamily="Segoe UI"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CabinetDesigner.Presentation;component/Views/Resources/ShellResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewProjectCommand}" />
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenProjectCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCommand}" />
        <KeyBinding Key="W" Modifiers="Ctrl" Command="{Binding CloseProjectCommand}" />
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding UndoCommand}" />
        <KeyBinding Key="Y" Modifiers="Ctrl" Command="{Binding RedoCommand}" />
    </Window.InputBindings>

    <DockPanel LastChildFill="True">
        <ProgressBar DockPanel.Dock="Top"
                     Height="3"
                     IsIndeterminate="True"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />
        <Menu DockPanel.Dock="Top"
              Background="{StaticResource ShellSurfaceBrush}"
              BorderBrush="{StaticResource ShellBorderSoftBrush}"
              BorderThickness="0,0,0,1">
            <MenuItem Header="_File">
                <MenuItem Header="_New Project"
                          Command="{Binding NewProjectCommand}"
                          InputGestureText="Ctrl+N" />
                <MenuItem Header="_Open Project"
                          Command="{Binding OpenProjectCommand}"
                          InputGestureText="Ctrl+O" />
                <MenuItem Header="_Save"
                          Command="{Binding SaveCommand}"
                          InputGestureText="Ctrl+S" />
                <Separator />
                <MenuItem Header="_Close Project"
                          Command="{Binding CloseProjectCommand}"
                          InputGestureText="Ctrl+W" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo"
                          Command="{Binding UndoCommand}"
                          InputGestureText="Ctrl+Z" />
                <MenuItem Header="_Redo"
                          Command="{Binding RedoCommand}"
                          InputGestureText="Ctrl+Y" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Workspace Layout" IsEnabled="False" />
                <MenuItem Header="Canvas Grid" IsEnabled="False" />
            </MenuItem>
            <MenuItem Header="_Design">
                <MenuItem Header="Selection Mode" IsEnabled="False" />
                <MenuItem Header="Run Layout Mode" IsEnabled="False" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="Validation" IsEnabled="False" />
                <MenuItem Header="Project Settings" IsEnabled="False" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="About" IsEnabled="False" />
            </MenuItem>
        </Menu>

        <views:ShellToolbarView DockPanel.Dock="Top" />
        <views:StatusBarView DockPanel.Dock="Bottom"
                             DataContext="{Binding StatusBar}" />

        <Grid Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" MinWidth="200" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GridSplitter Grid.Column="1"
                          Width="8"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ResizeDirection="Columns"
                          ResizeBehavior="PreviousAndNext"
                          Background="{StaticResource ShellBorderSoftBrush}"
                          Cursor="SizeWE" />

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MinHeight="120" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="260" MinHeight="120" />
                </Grid.RowDefinitions>

                <GridSplitter Grid.Row="1"
                              Height="8"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              ResizeDirection="Rows"
                              ResizeBehavior="PreviousAndNext"
                              Background="{StaticResource ShellBorderSoftBrush}"
                              Cursor="SizeNS" />

                <views:CatalogPanelView Grid.Row="0"
                                        DataContext="{Binding Catalog}" />

                <views:RunSummaryPanelView Grid.Row="2"
                                           DataContext="{Binding RunSummary}" />
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MinHeight="120" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="260" MinHeight="120" />
                </Grid.RowDefinitions>

                <GridSplitter Grid.Row="1"
                              Height="8"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              ResizeDirection="Rows"
                              ResizeBehavior="PreviousAndNext"
                              Background="{StaticResource ShellBorderSoftBrush}"
                              Cursor="SizeNS" />

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" MinWidth="400" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="2*" MinWidth="200" />
                    </Grid.ColumnDefinitions>

                    <GridSplitter Grid.Column="1"
                                  Width="8"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  ResizeDirection="Columns"
                                  ResizeBehavior="PreviousAndNext"
                                  Background="{StaticResource ShellBorderSoftBrush}"
                                  Cursor="SizeWE" />

                    <Border Grid.Column="0"
                            Style="{StaticResource ShellCardStyle}"
                            Padding="0">
                        <DockPanel>
                            <Border DockPanel.Dock="Top"
                                    Style="{StaticResource ShellPanelHeaderBorderStyle}"
                                    Margin="14,14,14,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding WorkspaceTitle}"
                                                   Style="{StaticResource ShellSectionTitleStyle}" />
                                        <TextBlock Text="{Binding WorkspaceSubtitle}"
                                                   Style="{StaticResource ShellSectionSubtitleStyle}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <Border Style="{StaticResource ShellBadgeStyle}"
                                                Margin="0,0,8,0">
                                            <TextBlock Text="{Binding CanvasCurrentMode}"
                                                       Style="{StaticResource ShellBadgeTextStyle}" />
                                        </Border>
                                        <Border Style="{StaticResource ShellBadgeStyle}">
                                            <TextBlock Text="{Binding SaveStateText}"
                                                       Style="{StaticResource ShellBadgeTextStyle}" />
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border Background="{StaticResource ShellSurfaceDarkBrush}"
                                    BorderBrush="#314152"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    ClipToBounds="True"
                                    Margin="14,0,14,14">
                                <ContentControl Content="{Binding Canvas.CanvasView}"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch" />
                            </Border>
                        </DockPanel>
                    </Border>

                    <views:PropertyInspectorView Grid.Column="2"
                                                 DataContext="{Binding PropertyInspector}" />
                </Grid>

                <views:IssuePanelView Grid.Row="2"
                                      DataContext="{Binding IssuePanel}" />
            </Grid>
        </Grid>
    </DockPanel>
</Window>
